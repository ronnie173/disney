package com.example.disneyproject.utils

import com.example.disneyproject.models.Result

/**
 * Cell click listener
 *
 * @constructor Cell click listener
 */
interface CellClickListener {
    fun onCellClickListener(result: Result)
}